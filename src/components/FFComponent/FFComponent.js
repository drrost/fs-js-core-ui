import classes from './FFComponent.module.css';

const FFComponent = () => {
    return (
        <div className={classes.container}>
            FFComponent
        </div>
    )
}

export default FFComponent
